package main

func main() {
	//create database bms
	//use bms

	/*

	create table author(
		id int primary key auto_increment,
		name char(255) not null	
	);

	create table publisher(
		id int primary key auto_increment,
		name char(255) not null	
	);

	create table book(
		id int primary key auto_increment,
		title char(255) not null,	
		price float(255, 2) not null,
		publish_date date not null,
		publisher_id int not null
	);

	create table book2author(
		id int primary key auto_increment,
		book_id int not null,
		author_id int not null
	);

	insert author(name) values("张三");
	insert author(name) values("李四");

	insert publisher(name) values("人民出版社");
	insert publisher(name) values("北京出版社");

	insert book(title, price, publish_date, publisher_id) values("西游记", 89.12, "2021-10-21", 1);
	insert book(title, price, publish_date, publisher_id) values("红楼梦", 19.12, "2020-7-21", 2);

	insert book2author(book_id, author_id) values(1, 1);
	insert book2author(book_id, author_id) values(2, 1);
	insert book2author(book_id, author_id) values(2, 2);

	go get github.com/jmoiron/sqlx
	*/
}